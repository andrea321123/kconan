// math.cn
// Collection of math utils

fun positivePow(n: i64, exp: u64): i64 {
    var result: i64 = 1;
    for (var i = 0; i < exp; i++) {
        result *= n;
    }

    return result;
}

fun abs(n: f64): f64 {
    if (n < 0) {
        return -n;
    }
    else {
        return n;
    }
}